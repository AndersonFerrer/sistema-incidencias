# Tasks: RBAC + catálogos públicos + scope AGENTE/USUARIO

**Capability**: `incidencias`
**Change**: `incidencias-rbac-agente`
**Date**: 2026-07-14
**Status**: ready-to-apply

Each task is sized to be reviewable in < 200 lines of diff. Tasks execute roughly in order, but T6 (frontend) and T7 (Postman) can be done in parallel with T5 (backend). Suggested execution: T1→T2→T3→T4→T5→(T6∥T7)→T8.

## T1. Add `validarAutenticado` to `PermisoAdministracionService`

- **Where**: `sistemaincidencias/src/main/java/com/integrador/sistemaincidencias/usuarios/service/PermisoAdministracionService.java`
- **Action**: Add method `public Usuario validarAutenticado(String authorizationHeader)` that delegates to `authService.obtenerUsuarioActual(authorizationHeader)` and returns the `Usuario` without role checks.
- **Do**: import nothing new; the file already has `AuthService` and `Usuario` imports.
- **Done when**: `./mvnw compile` passes and the new method is reachable from at least one call-site (T2).

## T2. Swap `validarAdministrador` → `validarAutenticado` in 4 catalog GETs

- **Where**:
  - `catalogos/controller/CategoriaController.java:30-36`
  - `catalogos/controller/AplicativoClienteController.java:32-38`
  - `catalogos/controller/EstadoProcesoController.java:30-36`
  - `catalogos/controller/EstadoAprobacionController.java:30-36`
- **Action**: Inside `listar(...)`, change `permisoAdministracionService.validarAdministrador(token)` to `permisoAdministracionService.validarAutenticado(token)`. Do NOT change `crear`/`actualizar` — those remain admin.
- **Done when**: `GET /api/categorias` returns 200 with any authenticated JWT (ADMIN, AGENTE, USUARIO). `POST /api/categorias` still returns 403 for AGENTE.

## T3. Add `GET /api/usuarios/agentes-asignables` endpoint

- **Where**:
  - `usuarios/controller/UsuarioController.java` (new method)
  - `usuarios/service/UsuarioService.java` (new method)
  - `usuarios/dao/UsuarioDao.java` (new method `listarAsignables()`)
  - `usuarios/sql/UsuarioSql.java` (new SQL constant `LISTAR_ASIGNABLES`)
- **Action**:
  - DAO SQL: `SELECT ... FROM usuarios u JOIN roles r ON u.rol_id = r.id WHERE r.codigo IN ('AGENTE','ADMINISTRADOR') AND u.activo = TRUE ORDER BY u.nombre ASC LIMIT 100`
  - Service method calls `validarAutenticado(header)` then `usuarioDao.listarAsignables()`, maps via existing `usuarioDtoMapper::toResponse`.
  - Controller method `@GetMapping("/agentes-asignables")` wired before/after `listar(...)` — exact position unimportant.
- **Done when**: `curl -H 'Authorization: Bearer <agente-token>' http://localhost:8080/api/usuarios/agentes-asignables` returns 200 with users whose rol is AGENTE or ADMINISTRADOR and `activo=true`. `curl ...<usuario-token>` returns same list.

## T4. Extend `IncidenciaFiltro` with `creadoPorUsuarioId`

- **Where**: `incidencias/dto/IncidenciaFiltro.java`
- **Action**: Add `private UUID creadoPorUsuarioId;` (Lombok `@Builder` already handles builder). Setter is auto-generated by Lombok `@Setter`.
- **Done when**: Type compiles. The field appears in builder but doesn't change existing call-sites.

## T5a. Update `IncidenciaDao` + `IncidenciaSql` for the new filter

- **Where**:
  - `incidencias/sql/IncidenciaSql.java`: add `creado_por_usuario_id` to the `SELECT` column list (already present per `IncidenciaSql.java:10,23`) AND add a new fragment constant `AND creado_por_usuario_id = ?` appended by the WHERE builder when filter is non-null.
  - `incidencias/dao/IncidenciaDao.java`: extend the WHERE-building method (currently `construirWhere` per audit) to handle the new field.
- **Action**: Find the existing pattern for adding optional predicates (e.g. `appendIfPresent` for `categoriaId`, `estadoProcesoId`, etc.) and mirror it for `creadoPorUsuarioId`. Add binding to the parameter counter.
- **Done when**: `./mvnw compile` passes AND a manual `psql` query for the same WHERE returns the expected rows.

## T5b. Inject auth into `IncidenciaController.listar`

- **Where**: `incidencias/controller/IncidenciaController.java:48-74`
- **Action**:
  - Add `@RequestHeader("Authorization") String token` to the method signature (after all `@RequestParam`).
  - Inject `AuthService` (constructor via Lombok `@RequiredArgsConstructor`).
  - After building the `IncidenciaFiltro`, add:
    ```java
    Usuario actual = authService.obtenerUsuarioActual(token);
    if (!actual.getRol().esAdministrador()) {
        if (actual.getRol().esAgente()) {
            filtro.setAsignadoA(actual.getId());
        } else {
            filtro.setCreadoPorUsuarioId(actual.getId());
        }
    }
    ```
- **Done when**: AGENTE test scenario returns only own; ADMIN returns full.

## T5c. Add `validarAlcance` helper and wire into all state-changing methods

- **Where**: `incidencias/service/IncidenciaService.java`
- **Action**:
  - Add the private helper `validarAlcance(Usuario actual, Incidencia target, String metodo)` per design §2.
  - At the top of each method (`obtener`, `actualizar`, `actualizarConArchivos`, `cambiarEstado`, `aprobar`, `rechazar`, `agregarComentario`, `agregarAdjunto`, `agregarAdjuntos`): load the `Incidencia` first, then call `validarAlcance(actual, incidencia, "<nombreMetodo>")`.
  - In `eliminar`: skip `validarAlcance`; throw `AccesoDenegadoException` for any non-admin role.
- **Done when**: Spec scenarios 5a-5h pass.

## T6. Frontend cleanup — drop gate + add new service method

- **Where** (4 files):
  - `frontend/src/services/usuarios-service.ts`: add `listarAgentesAsignables(signal?: AbortSignal)` method.
  - `frontend/src/pages/incidencias/index.tsx`: remove `if (currentUserIsAdmin)` gate in `loadCatalogos`; swap `usuariosService.listar()` → `usuariosService.listarAsignables()`.
  - `frontend/src/pages/incidencias/components/nueva-incidencia-view.tsx`: same swap.
  - `frontend/src/pages/incidencias/components/editar-incidencia-dialog.tsx`: same swap.
- **Action**:
  - Add the new method using the pattern of `roles-service.ts:listar(signal)` (single-line wrapper).
  - In the 3 callers, find/replace `usuariosService.listar()` (or `usuariosService.listar(filtros, signal)` if exists) with the new method.
- **Done when**: `npm run build` passes with no new errors. Manual: AGENTE login → `/incidencias` loads, badges show names instead of `—`.

## T7. Update Postman collection

- **Where**: `sistemaincidencias/postman/SistemaIncidencias.postman_collection.json`
- **Action**:
  - Add new request entry: `GET /api/usuarios/agentes-asignables` under the "Usuarios" folder; auth = "Bearer token"; example response = array of 2-3 users with rol AGENTE.
  - In the 4 existing catalog entries: change the auth note from "ADMINISTRADOR only" to "authenticated (any role)".
- **Done when**: JSON parses (validator OK); visual diff in Postman shows the new entry and 4 auth-note changes.

## T8. Lint + Build verification

- **Where**:
  - `cd frontend && npm run lint`
  - `cd frontend && npm run build`
  - `cd sistemaincidencias && ./mvnw compile` (or `mvn compile`)
- **Action**: All must pass with no new errors introduced by this change. Pre-existing errors in master (4 build warnings related to unused vars) remain out of scope and are not regressions.
- **Done when**: Each command exits 0 OR exits with only the same 4 pre-existing errors documented in the audit.

## Definition of done

- T1-T8 all complete
- Manual smoke covers: ADMIN sees all, AGENTE sees only assigned, USUARIO sees only created-by-him. Each role cannot edit/change-state on out-of-scope resources.
- Updated `proposal.md` Status = applied → ready for verify.
- Create `openspec/changes/incidencias-rbac-agente/apply-progress.md` summarizing timestamps per task and any deviations encountered.
